# Global Agent Instructions

## Language

- 日本語で応答すること
- コード・識別子・ファイル名・コードコメントは英語で記述すること

## セキュリティ

- 認証情報（APIキー、パスワード、トークン等）はコード・ログ・コミットに含めず、環境変数や専用のシークレット管理サービス（AWS Secrets Manager, Parameter Store等）を利用すること
- ユーザー入力は常に検証・サニタイズし、SQLインジェクション・XSS・CSRF等の脆弱性に配慮すること
- 最小権限の原則に従い、IAMロール等の権限設定を必要最小限にすること
- 依存パッケージの脆弱性を定期的に確認すること（`npm audit`, `pip-audit` 等）
- 個人情報・機密情報は保存時・通信時ともに暗号化すること
- 認証・認可・権限設定に関わる変更は、実装前にレビュー観点を明示すること

## 開発プロセス：仕様駆動開発（Spec-Driven Development）

実装よりも仕様の確定を優先する。仕様が曖昧なまま実装を進めず、不明点はユーザーに確認すること

各機能・修正について以下3点を作成し、ユーザーの承認を得てから実装に着手する。実装内容が仕様と乖離する場合は、先に仕様書を更新してから実装を修正すること

| ファイル名        | 内容                                              |
| ----------------- | ------------------------------------------------- |
| `requirements.md` | 要求事項・受け入れ基準                            |
| `design.md`       | 設計内容（アーキテクチャ、データ構造、API仕様等） |
| `tasks.md`        | 実装タスクの分解・順序・依存関係                  |

## ログ出力

- `console.log` 等、追跡できず握りつぶされるログ出力を残さないこと
- 標準的なロガー（SLF4J/Logback, `logging`パッケージ, `dart:developer`等）を使用し、ログレベル（ERROR/WARN/INFO/DEBUG）を使い分けること
- 本番相当環境ではログを永続化し、事後調査できる状態にすること
- ログには発生箇所・原因を特定できる情報を含め、パスワード等の機密情報は出力しないこと
- デバッグ用の一時ログはコミット前に削除するか適切なレベルに変更すること

## 例外処理

- 例外を空の`catch`等で握りつぶさず、必ずログ出力または呼び出し元への伝播を行うこと
- ユーザー向けにはわかりやすいエラーメッセージを表示し、スタックトレース等の内部詳細はログ・ログ収集基盤にのみ記録すること
- 回復可能な例外はリトライ等で対処し、予期しない例外も共通ハンドラで必ず捕捉すること
- トレーサビリティのため、リクエストID等の識別子をログとエラーメッセージに含めることを検討すること

## テスト自動化

- 実装タスク（`tasks.md`）には対応するテストタスクを必ず含め、テストが存在しない・失敗する状態で完了扱いにしないこと
- テストピラミッド（単体＞結合＞E2E）を意識し、単体テストを厚めにすること
- CIでテストを自動実行し、テストが通らない変更はマージしないこと
- バグ修正時は再現テストを先に追加してから修正すること
- カバレッジの著しい低下がないか継続的に確認すること（数値目標は `requirements.md` で定義する）
- 具体的な手法・ツールは各スタック配下の `CLAUDE.md` を参照すること

## スタック別ルール

各スタック固有のルールは、それぞれのフォルダ配下の `CLAUDE.md` を参照・遵守すること

- `aws-cdk/CLAUDE.md` : AWS CDK に関するルール
- `flutter/CLAUDE.md` : Flutter に関するルール
- `springboot/CLAUDE.md` : Spring Boot に関するルール
