# Global Agent Instructions

## Language

- 日本語で応答すること
- コード、識別子、ファイル名は英語で記述すること
- コードコメントは英語で記述すること

## セキュリティ

- 認証情報（APIキー、パスワード、トークン、シークレット等）をコードやログ、コミットに含めないこと
- 環境変数や専用のシークレット管理サービス（AWS Secrets Manager, Parameter Store等）を利用すること
- ユーザー入力は常に信頼せず、バリデーション・サニタイズを行うこと
- SQLインジェクション、XSS、CSRF等の一般的な脆弱性に配慮した実装を行うこと
- 最小権限の原則（Principle of Least Privilege）に従い、IAMロールや権限設定を必要最小限にすること
- 依存パッケージは既知の脆弱性がないか定期的に確認すること（`npm audit`, `pip-audit` 等）
- 個人情報・機密情報を扱う場合は暗号化（保存時・通信時）を徹底すること
- セキュリティに関わる変更（認証・認可・権限設定など）は、実装前に必ずレビュー観点を明示すること

## 開発プロセス：仕様駆動開発（Spec-Driven Development）

本プロジェクトでは仕様駆動開発を採用する。実装よりも仕様の確定を優先すること。

### 基本原則

- **修正・実装を行う前に、必ず仕様を確定させること**
- 仕様が曖昧なまま実装を進めてはならない。不明点があればユーザーに確認すること
- 仕様書を先に作成・更新し、レビュー（ユーザー承認）を得てから実装に着手すること

### 仕様書の構成

各機能・修正には以下3種類の仕様書を作成する。

| ファイル名 | 内容 |
|---|---|
| `requirements.md` | 要求事項・要件定義（何を実現したいか、受け入れ基準） |
| `design.md` | 設計内容（アーキテクチャ、データ構造、API仕様、画面設計など） |
| `tasks.md` | 実装タスクの分解（作業項目、順序、依存関係） |

### 作業フロー

1. `requirements.md` を作成・更新し、要件を明確化する
2. `design.md` を作成・更新し、設計方針を明確化する
3. `tasks.md` を作成し、実装タスクに分解する
4. 上記3点についてユーザーの承認を得る
5. 承認後、タスクに従って実装を行う
6. 実装内容が仕様と乖離する場合は、先に仕様書を更新してから実装を修正すること

## ログ出力

- `console.log` やその場限りの標準出力デバッグ等、あとから追跡できない・握りつぶされるログ出力を残さないこと
- ロギングは各言語・フレームワークの標準的なロガー（例: SLF4J/Logback, `logging`パッケージ, `dart:developer`/専用ロガー等）を使用し、ログレベル（ERROR/WARN/INFO/DEBUG）を適切に使い分けること
- 本番相当環境ではログをファイルまたはログ収集基盤（CloudWatch Logs等）に永続化し、事後調査できる状態にすること
- ログには「何が」「なぜ」発生したか特定できる情報（処理名、識別子、エラー内容等）を含めること
- パスワード・トークン・個人情報等の機密情報はログに出力しないこと
- デバッグ目的で一時的に追加したログ出力は、コミット・マージ前に削除するか、適切なログレベル（DEBUG等）に変更すること

## 例外処理

- 例外・エラーを空の`catch`ブロック等で握りつぶさないこと。catchした場合は必ずログ出力するか、呼び出し元に再スロー・伝播させること
- ユーザー向けの画面には、事象が把握できる分かりやすいエラーメッセージを表示すること（内部の技術的詳細・スタックトレースはそのまま表示しない）
- サーバー・システム内部の詳細（スタックトレース、例外内容、発生箇所）はログファイルまたはログ収集基盤に記録し、後から事象を追跡できるようにすること
- 例外の種類に応じて適切なハンドリングを行うこと（回復可能なものはリトライ・代替処理、回復不能なものはユーザーへの通知とログ記録）
- 予期しない例外（想定外エラー）についても、共通のハンドラ等で必ず捕捉し、未処理のままアプリケーションが不可解に落ちることがないようにすること
- エラー発生時のトレーサビリティを高めるため、リクエストID・トランザクションID等の識別子をログとエラーメッセージの両方に含めることを検討すること

## テスト自動化

品質を担保するため、テスト自動化を必須とする。

### 基本方針

- 実装タスク（`tasks.md`）には、対応するテストタスクを必ず含めること
- テストが存在しない、または失敗する状態でタスクを完了扱いにしないこと
- テストピラミッド（単体テスト＞結合テスト＞E2Eテスト）を意識し、単体テストを厚めに、E2E/UIテストは主要シナリオに絞ること
- CI（GitHub Actions等）でテストを自動実行し、テストが通らない変更はマージしないこと
- リグレッション（デグレ）を防ぐため、バグ修正時は再現テストを先に追加してから修正すること
- カバレッジ計測を行い、著しい低下がないか継続的に確認すること（数値目標はプロジェクトごとに `requirements.md` で定義する）
- 各スタックにおけるテストの具体的な手法・ツールは、それぞれのフォルダ配下の `CLAUDE.md` を参照すること

## スタック別ルール

各スタック固有のルールは、それぞれのフォルダ配下の `CLAUDE.md` を参照・遵守すること。

- `aws-cdk/CLAUDE.md` : AWS CDK に関するルール
- `flutter/CLAUDE.md` : Flutter に関するルール
- `springboot/CLAUDE.md` : Spring Boot に関するルール
